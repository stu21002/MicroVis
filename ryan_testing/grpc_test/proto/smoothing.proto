syntax = "proto3";

message Output {
    string value = 1;
}

message Empty{}

service SmoothingServices{
    rpc computeGuassianBlur(Empty) returns (Output) {}
    rpc computeBlockSmoothing(Empty) returns (Output) {}
}